# Shell Notes

## Machine-Local Overrides

This package supports local overrides via an untracked `localrc` file. Use `localrc.example` as a template for secrets, machine-specific environment variables, and personal aliases.
